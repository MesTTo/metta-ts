// SPDX-FileCopyrightText: 2026 SingularityNET Foundation (generated from singnet/das-proto)
//
// SPDX-License-Identifier: MIT

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.21.12
// source: atom_space_node.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Ack, Empty } from "./common";

export const protobufPackage = "dasproto";

export interface MessageData {
  command: string;
  args: string[];
  sender: string;
  isBroadcast: boolean;
  visitedRecipients: string[];
}

function createBaseMessageData(): MessageData {
  return { command: "", args: [], sender: "", isBroadcast: false, visitedRecipients: [] };
}

export const MessageData: MessageFns<MessageData> = {
  encode(message: MessageData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.command !== "") {
      writer.uint32(10).string(message.command);
    }
    for (const v of message.args) {
      writer.uint32(18).string(v!);
    }
    if (message.sender !== "") {
      writer.uint32(26).string(message.sender);
    }
    if (message.isBroadcast !== false) {
      writer.uint32(32).bool(message.isBroadcast);
    }
    for (const v of message.visitedRecipients) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.command = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.args.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isBroadcast = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.visitedRecipients.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageData {
    return {
      command: isSet(object.command) ? globalThis.String(object.command) : "",
      args: globalThis.Array.isArray(object?.args) ? object.args.map((e: any) => globalThis.String(e)) : [],
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      isBroadcast: isSet(object.isBroadcast)
        ? globalThis.Boolean(object.isBroadcast)
        : isSet(object.is_broadcast)
        ? globalThis.Boolean(object.is_broadcast)
        : false,
      visitedRecipients: globalThis.Array.isArray(object?.visitedRecipients)
        ? object.visitedRecipients.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.visited_recipients)
        ? object.visited_recipients.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: MessageData): unknown {
    const obj: any = {};
    if (message.command !== "") {
      obj.command = message.command;
    }
    if (message.args?.length) {
      obj.args = message.args;
    }
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.isBroadcast !== false) {
      obj.isBroadcast = message.isBroadcast;
    }
    if (message.visitedRecipients?.length) {
      obj.visitedRecipients = message.visitedRecipients;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageData>, I>>(base?: I): MessageData {
    return MessageData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageData>, I>>(object: I): MessageData {
    const message = createBaseMessageData();
    message.command = object.command ?? "";
    message.args = object.args?.map((e) => e) || [];
    message.sender = object.sender ?? "";
    message.isBroadcast = object.isBroadcast ?? false;
    message.visitedRecipients = object.visitedRecipients?.map((e) => e) || [];
    return message;
  },
};

export type AtomSpaceNodeService = typeof AtomSpaceNodeService;
export const AtomSpaceNodeService = {
  ping: {
    path: "/dasproto.AtomSpaceNode/ping" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    requestDeserialize: (value: Buffer): Empty => Empty.decode(value),
    responseSerialize: (value: Ack): Buffer => Buffer.from(Ack.encode(value).finish()),
    responseDeserialize: (value: Buffer): Ack => Ack.decode(value),
  },
  executeMessage: {
    path: "/dasproto.AtomSpaceNode/execute_message" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: MessageData): Buffer => Buffer.from(MessageData.encode(value).finish()),
    requestDeserialize: (value: Buffer): MessageData => MessageData.decode(value),
    responseSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    responseDeserialize: (value: Buffer): Empty => Empty.decode(value),
  },
} as const;

export interface AtomSpaceNodeServer extends UntypedServiceImplementation {
  ping: handleUnaryCall<Empty, Ack>;
  executeMessage: handleUnaryCall<MessageData, Empty>;
}

export interface AtomSpaceNodeClient extends Client {
  ping(request: Empty, callback: (error: ServiceError | null, response: Ack) => void): ClientUnaryCall;
  ping(
    request: Empty,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  ping(
    request: Empty,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  executeMessage(
    request: MessageData,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  executeMessage(
    request: MessageData,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  executeMessage(
    request: MessageData,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
}

export const AtomSpaceNodeClient = makeGenericClientConstructor(
  AtomSpaceNodeService,
  "dasproto.AtomSpaceNode",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): AtomSpaceNodeClient;
  service: typeof AtomSpaceNodeService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
