// SPDX-FileCopyrightText: 2026 SingularityNET Foundation (generated from singnet/das-proto)
//
// SPDX-License-Identifier: MIT

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.21.12
// source: attention_broker.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Ack, Empty } from "./common";

export const protobufPackage = "dasproto";

export interface HandleList {
  list: string[];
  requestType: number;
  hebbianNetwork: string;
  context: string;
}

export interface HandleListList {
  list: HandleList[];
  requestType: number;
  hebbianNetwork: string;
  context: string;
}

export interface HandleCount {
  map: { [key: string]: number };
  requestType: number;
  hebbianNetwork: string;
  context: string;
}

export interface HandleCount_MapEntry {
  key: string;
  value: number;
}

export interface ImportanceList {
  list: number[];
}

export interface Parameters {
  rentRate: number;
  spreadingRateLowerbound: number;
  spreadingRateUpperbound: number;
}

export interface ContextPersistence {
  context: string;
  fileName: string;
}

function createBaseHandleList(): HandleList {
  return { list: [], requestType: 0, hebbianNetwork: "0", context: "" };
}

export const HandleList: MessageFns<HandleList> = {
  encode(message: HandleList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.list) {
      writer.uint32(10).string(v!);
    }
    if (message.requestType !== 0) {
      writer.uint32(16).int32(message.requestType);
    }
    if (message.hebbianNetwork !== "0") {
      writer.uint32(24).int64(message.hebbianNetwork);
    }
    if (message.context !== "") {
      writer.uint32(34).string(message.context);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HandleList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHandleList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.list.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.requestType = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hebbianNetwork = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.context = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HandleList {
    return {
      list: globalThis.Array.isArray(object?.list) ? object.list.map((e: any) => globalThis.String(e)) : [],
      requestType: isSet(object.requestType)
        ? globalThis.Number(object.requestType)
        : isSet(object.request_type)
        ? globalThis.Number(object.request_type)
        : 0,
      hebbianNetwork: isSet(object.hebbianNetwork)
        ? globalThis.String(object.hebbianNetwork)
        : isSet(object.hebbian_network)
        ? globalThis.String(object.hebbian_network)
        : "0",
      context: isSet(object.context) ? globalThis.String(object.context) : "",
    };
  },

  toJSON(message: HandleList): unknown {
    const obj: any = {};
    if (message.list?.length) {
      obj.list = message.list;
    }
    if (message.requestType !== 0) {
      obj.requestType = Math.round(message.requestType);
    }
    if (message.hebbianNetwork !== "0") {
      obj.hebbianNetwork = message.hebbianNetwork;
    }
    if (message.context !== "") {
      obj.context = message.context;
    }
    return obj;
  },

  create(base?: DeepPartial<HandleList>): HandleList {
    return HandleList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HandleList>): HandleList {
    const message = createBaseHandleList();
    message.list = object.list?.map((e) => e) || [];
    message.requestType = object.requestType ?? 0;
    message.hebbianNetwork = object.hebbianNetwork ?? "0";
    message.context = object.context ?? "";
    return message;
  },
};

function createBaseHandleListList(): HandleListList {
  return { list: [], requestType: 0, hebbianNetwork: "0", context: "" };
}

export const HandleListList: MessageFns<HandleListList> = {
  encode(message: HandleListList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.list) {
      HandleList.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.requestType !== 0) {
      writer.uint32(16).int32(message.requestType);
    }
    if (message.hebbianNetwork !== "0") {
      writer.uint32(24).int64(message.hebbianNetwork);
    }
    if (message.context !== "") {
      writer.uint32(34).string(message.context);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HandleListList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHandleListList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.list.push(HandleList.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.requestType = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hebbianNetwork = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.context = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HandleListList {
    return {
      list: globalThis.Array.isArray(object?.list) ? object.list.map((e: any) => HandleList.fromJSON(e)) : [],
      requestType: isSet(object.requestType)
        ? globalThis.Number(object.requestType)
        : isSet(object.request_type)
        ? globalThis.Number(object.request_type)
        : 0,
      hebbianNetwork: isSet(object.hebbianNetwork)
        ? globalThis.String(object.hebbianNetwork)
        : isSet(object.hebbian_network)
        ? globalThis.String(object.hebbian_network)
        : "0",
      context: isSet(object.context) ? globalThis.String(object.context) : "",
    };
  },

  toJSON(message: HandleListList): unknown {
    const obj: any = {};
    if (message.list?.length) {
      obj.list = message.list.map((e) => HandleList.toJSON(e));
    }
    if (message.requestType !== 0) {
      obj.requestType = Math.round(message.requestType);
    }
    if (message.hebbianNetwork !== "0") {
      obj.hebbianNetwork = message.hebbianNetwork;
    }
    if (message.context !== "") {
      obj.context = message.context;
    }
    return obj;
  },

  create(base?: DeepPartial<HandleListList>): HandleListList {
    return HandleListList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HandleListList>): HandleListList {
    const message = createBaseHandleListList();
    message.list = object.list?.map((e) => HandleList.fromPartial(e)) || [];
    message.requestType = object.requestType ?? 0;
    message.hebbianNetwork = object.hebbianNetwork ?? "0";
    message.context = object.context ?? "";
    return message;
  },
};

function createBaseHandleCount(): HandleCount {
  return { map: {}, requestType: 0, hebbianNetwork: "0", context: "" };
}

export const HandleCount: MessageFns<HandleCount> = {
  encode(message: HandleCount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.map).forEach(([key, value]: [string, number]) => {
      HandleCount_MapEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    if (message.requestType !== 0) {
      writer.uint32(16).int32(message.requestType);
    }
    if (message.hebbianNetwork !== "0") {
      writer.uint32(24).int64(message.hebbianNetwork);
    }
    if (message.context !== "") {
      writer.uint32(34).string(message.context);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HandleCount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHandleCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = HandleCount_MapEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.map[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.requestType = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.hebbianNetwork = reader.int64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.context = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HandleCount {
    return {
      map: isObject(object.map)
        ? (globalThis.Object.entries(object.map) as [string, any][]).reduce(
          (acc: { [key: string]: number }, [key, value]: [string, any]) => {
            acc[key] = globalThis.Number(value);
            return acc;
          },
          {},
        )
        : {},
      requestType: isSet(object.requestType)
        ? globalThis.Number(object.requestType)
        : isSet(object.request_type)
        ? globalThis.Number(object.request_type)
        : 0,
      hebbianNetwork: isSet(object.hebbianNetwork)
        ? globalThis.String(object.hebbianNetwork)
        : isSet(object.hebbian_network)
        ? globalThis.String(object.hebbian_network)
        : "0",
      context: isSet(object.context) ? globalThis.String(object.context) : "",
    };
  },

  toJSON(message: HandleCount): unknown {
    const obj: any = {};
    if (message.map) {
      const entries = globalThis.Object.entries(message.map) as [string, number][];
      if (entries.length > 0) {
        obj.map = {};
        entries.forEach(([k, v]) => {
          obj.map[k] = Math.round(v);
        });
      }
    }
    if (message.requestType !== 0) {
      obj.requestType = Math.round(message.requestType);
    }
    if (message.hebbianNetwork !== "0") {
      obj.hebbianNetwork = message.hebbianNetwork;
    }
    if (message.context !== "") {
      obj.context = message.context;
    }
    return obj;
  },

  create(base?: DeepPartial<HandleCount>): HandleCount {
    return HandleCount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HandleCount>): HandleCount {
    const message = createBaseHandleCount();
    message.map = (globalThis.Object.entries(object.map ?? {}) as [string, number][]).reduce(
      (acc: { [key: string]: number }, [key, value]: [string, number]) => {
        if (value !== undefined) {
          acc[key] = globalThis.Number(value);
        }
        return acc;
      },
      {},
    );
    message.requestType = object.requestType ?? 0;
    message.hebbianNetwork = object.hebbianNetwork ?? "0";
    message.context = object.context ?? "";
    return message;
  },
};

function createBaseHandleCount_MapEntry(): HandleCount_MapEntry {
  return { key: "", value: 0 };
}

export const HandleCount_MapEntry: MessageFns<HandleCount_MapEntry> = {
  encode(message: HandleCount_MapEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== 0) {
      writer.uint32(16).uint32(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HandleCount_MapEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHandleCount_MapEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HandleCount_MapEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.Number(object.value) : 0,
    };
  },

  toJSON(message: HandleCount_MapEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== 0) {
      obj.value = Math.round(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<HandleCount_MapEntry>): HandleCount_MapEntry {
    return HandleCount_MapEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HandleCount_MapEntry>): HandleCount_MapEntry {
    const message = createBaseHandleCount_MapEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? 0;
    return message;
  },
};

function createBaseImportanceList(): ImportanceList {
  return { list: [] };
}

export const ImportanceList: MessageFns<ImportanceList> = {
  encode(message: ImportanceList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.list) {
      writer.float(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportanceList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportanceList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 13) {
            message.list.push(reader.float());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.list.push(reader.float());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportanceList {
    return { list: globalThis.Array.isArray(object?.list) ? object.list.map((e: any) => globalThis.Number(e)) : [] };
  },

  toJSON(message: ImportanceList): unknown {
    const obj: any = {};
    if (message.list?.length) {
      obj.list = message.list;
    }
    return obj;
  },

  create(base?: DeepPartial<ImportanceList>): ImportanceList {
    return ImportanceList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImportanceList>): ImportanceList {
    const message = createBaseImportanceList();
    message.list = object.list?.map((e) => e) || [];
    return message;
  },
};

function createBaseParameters(): Parameters {
  return { rentRate: 0, spreadingRateLowerbound: 0, spreadingRateUpperbound: 0 };
}

export const Parameters: MessageFns<Parameters> = {
  encode(message: Parameters, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rentRate !== 0) {
      writer.uint32(13).float(message.rentRate);
    }
    if (message.spreadingRateLowerbound !== 0) {
      writer.uint32(21).float(message.spreadingRateLowerbound);
    }
    if (message.spreadingRateUpperbound !== 0) {
      writer.uint32(29).float(message.spreadingRateUpperbound);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Parameters {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParameters();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.rentRate = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.spreadingRateLowerbound = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.spreadingRateUpperbound = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Parameters {
    return {
      rentRate: isSet(object.rentRate)
        ? globalThis.Number(object.rentRate)
        : isSet(object.rent_rate)
        ? globalThis.Number(object.rent_rate)
        : 0,
      spreadingRateLowerbound: isSet(object.spreadingRateLowerbound)
        ? globalThis.Number(object.spreadingRateLowerbound)
        : isSet(object.spreading_rate_lowerbound)
        ? globalThis.Number(object.spreading_rate_lowerbound)
        : 0,
      spreadingRateUpperbound: isSet(object.spreadingRateUpperbound)
        ? globalThis.Number(object.spreadingRateUpperbound)
        : isSet(object.spreading_rate_upperbound)
        ? globalThis.Number(object.spreading_rate_upperbound)
        : 0,
    };
  },

  toJSON(message: Parameters): unknown {
    const obj: any = {};
    if (message.rentRate !== 0) {
      obj.rentRate = message.rentRate;
    }
    if (message.spreadingRateLowerbound !== 0) {
      obj.spreadingRateLowerbound = message.spreadingRateLowerbound;
    }
    if (message.spreadingRateUpperbound !== 0) {
      obj.spreadingRateUpperbound = message.spreadingRateUpperbound;
    }
    return obj;
  },

  create(base?: DeepPartial<Parameters>): Parameters {
    return Parameters.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Parameters>): Parameters {
    const message = createBaseParameters();
    message.rentRate = object.rentRate ?? 0;
    message.spreadingRateLowerbound = object.spreadingRateLowerbound ?? 0;
    message.spreadingRateUpperbound = object.spreadingRateUpperbound ?? 0;
    return message;
  },
};

function createBaseContextPersistence(): ContextPersistence {
  return { context: "", fileName: "" };
}

export const ContextPersistence: MessageFns<ContextPersistence> = {
  encode(message: ContextPersistence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.context !== "") {
      writer.uint32(10).string(message.context);
    }
    if (message.fileName !== "") {
      writer.uint32(18).string(message.fileName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContextPersistence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContextPersistence();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.context = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContextPersistence {
    return {
      context: isSet(object.context) ? globalThis.String(object.context) : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
    };
  },

  toJSON(message: ContextPersistence): unknown {
    const obj: any = {};
    if (message.context !== "") {
      obj.context = message.context;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    return obj;
  },

  create(base?: DeepPartial<ContextPersistence>): ContextPersistence {
    return ContextPersistence.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContextPersistence>): ContextPersistence {
    const message = createBaseContextPersistence();
    message.context = object.context ?? "";
    message.fileName = object.fileName ?? "";
    return message;
  },
};

export type AttentionBrokerService = typeof AttentionBrokerService;
export const AttentionBrokerService = {
  ping: {
    path: "/dasproto.AttentionBroker/ping" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    requestDeserialize: (value: Buffer): Empty => Empty.decode(value),
    responseSerialize: (value: Ack): Buffer => Buffer.from(Ack.encode(value).finish()),
    responseDeserialize: (value: Buffer): Ack => Ack.decode(value),
  },
  stimulate: {
    path: "/dasproto.AttentionBroker/stimulate" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HandleCount): Buffer => Buffer.from(HandleCount.encode(value).finish()),
    requestDeserialize: (value: Buffer): HandleCount => HandleCount.decode(value),
    responseSerialize: (value: Ack): Buffer => Buffer.from(Ack.encode(value).finish()),
    responseDeserialize: (value: Buffer): Ack => Ack.decode(value),
  },
  correlate: {
    path: "/dasproto.AttentionBroker/correlate" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HandleList): Buffer => Buffer.from(HandleList.encode(value).finish()),
    requestDeserialize: (value: Buffer): HandleList => HandleList.decode(value),
    responseSerialize: (value: Ack): Buffer => Buffer.from(Ack.encode(value).finish()),
    responseDeserialize: (value: Buffer): Ack => Ack.decode(value),
  },
  asymmetricCorrelate: {
    path: "/dasproto.AttentionBroker/asymmetric_correlate" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HandleList): Buffer => Buffer.from(HandleList.encode(value).finish()),
    requestDeserialize: (value: Buffer): HandleList => HandleList.decode(value),
    responseSerialize: (value: Ack): Buffer => Buffer.from(Ack.encode(value).finish()),
    responseDeserialize: (value: Buffer): Ack => Ack.decode(value),
  },
  getImportance: {
    path: "/dasproto.AttentionBroker/get_importance" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HandleList): Buffer => Buffer.from(HandleList.encode(value).finish()),
    requestDeserialize: (value: Buffer): HandleList => HandleList.decode(value),
    responseSerialize: (value: ImportanceList): Buffer => Buffer.from(ImportanceList.encode(value).finish()),
    responseDeserialize: (value: Buffer): ImportanceList => ImportanceList.decode(value),
  },
  setDeterminers: {
    path: "/dasproto.AttentionBroker/set_determiners" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: HandleListList): Buffer => Buffer.from(HandleListList.encode(value).finish()),
    requestDeserialize: (value: Buffer): HandleListList => HandleListList.decode(value),
    responseSerialize: (value: Ack): Buffer => Buffer.from(Ack.encode(value).finish()),
    responseDeserialize: (value: Buffer): Ack => Ack.decode(value),
  },
  setParameters: {
    path: "/dasproto.AttentionBroker/set_parameters" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: Parameters): Buffer => Buffer.from(Parameters.encode(value).finish()),
    requestDeserialize: (value: Buffer): Parameters => Parameters.decode(value),
    responseSerialize: (value: Ack): Buffer => Buffer.from(Ack.encode(value).finish()),
    responseDeserialize: (value: Buffer): Ack => Ack.decode(value),
  },
  saveContext: {
    path: "/dasproto.AttentionBroker/save_context" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ContextPersistence): Buffer => Buffer.from(ContextPersistence.encode(value).finish()),
    requestDeserialize: (value: Buffer): ContextPersistence => ContextPersistence.decode(value),
    responseSerialize: (value: Ack): Buffer => Buffer.from(Ack.encode(value).finish()),
    responseDeserialize: (value: Buffer): Ack => Ack.decode(value),
  },
  dropAndLoadContext: {
    path: "/dasproto.AttentionBroker/drop_and_load_context" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ContextPersistence): Buffer => Buffer.from(ContextPersistence.encode(value).finish()),
    requestDeserialize: (value: Buffer): ContextPersistence => ContextPersistence.decode(value),
    responseSerialize: (value: Ack): Buffer => Buffer.from(Ack.encode(value).finish()),
    responseDeserialize: (value: Buffer): Ack => Ack.decode(value),
  },
} as const;

export interface AttentionBrokerServer extends UntypedServiceImplementation {
  ping: handleUnaryCall<Empty, Ack>;
  stimulate: handleUnaryCall<HandleCount, Ack>;
  correlate: handleUnaryCall<HandleList, Ack>;
  asymmetricCorrelate: handleUnaryCall<HandleList, Ack>;
  getImportance: handleUnaryCall<HandleList, ImportanceList>;
  setDeterminers: handleUnaryCall<HandleListList, Ack>;
  setParameters: handleUnaryCall<Parameters, Ack>;
  saveContext: handleUnaryCall<ContextPersistence, Ack>;
  dropAndLoadContext: handleUnaryCall<ContextPersistence, Ack>;
}

export interface AttentionBrokerClient extends Client {
  ping(request: Empty, callback: (error: ServiceError | null, response: Ack) => void): ClientUnaryCall;
  ping(
    request: Empty,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  ping(
    request: Empty,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  stimulate(request: HandleCount, callback: (error: ServiceError | null, response: Ack) => void): ClientUnaryCall;
  stimulate(
    request: HandleCount,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  stimulate(
    request: HandleCount,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  correlate(request: HandleList, callback: (error: ServiceError | null, response: Ack) => void): ClientUnaryCall;
  correlate(
    request: HandleList,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  correlate(
    request: HandleList,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  asymmetricCorrelate(
    request: HandleList,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  asymmetricCorrelate(
    request: HandleList,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  asymmetricCorrelate(
    request: HandleList,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  getImportance(
    request: HandleList,
    callback: (error: ServiceError | null, response: ImportanceList) => void,
  ): ClientUnaryCall;
  getImportance(
    request: HandleList,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ImportanceList) => void,
  ): ClientUnaryCall;
  getImportance(
    request: HandleList,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ImportanceList) => void,
  ): ClientUnaryCall;
  setDeterminers(
    request: HandleListList,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  setDeterminers(
    request: HandleListList,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  setDeterminers(
    request: HandleListList,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  setParameters(request: Parameters, callback: (error: ServiceError | null, response: Ack) => void): ClientUnaryCall;
  setParameters(
    request: Parameters,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  setParameters(
    request: Parameters,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  saveContext(
    request: ContextPersistence,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  saveContext(
    request: ContextPersistence,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  saveContext(
    request: ContextPersistence,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  dropAndLoadContext(
    request: ContextPersistence,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  dropAndLoadContext(
    request: ContextPersistence,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
  dropAndLoadContext(
    request: ContextPersistence,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Ack) => void,
  ): ClientUnaryCall;
}

export const AttentionBrokerClient = makeGenericClientConstructor(
  AttentionBrokerService,
  "dasproto.AttentionBroker",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): AttentionBrokerClient;
  service: typeof AttentionBrokerService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
